# Catalog Components

- SearchBar.jsx: filter form for catalog searching (query required; optional genre, artist, album).
- SearchResults.jsx: renders result list with basic pagination controls and a placeholder "Add to playlist" action.
