# WebFrontend environment variables (copy to .env for local development)
# Note: Do not commit your real .env to source control.

# Base URL for API requests used by axios in src/api/client.js.
# Leave blank to use CRA dev proxy (src/setupProxy.js) at /api/* pointing to BACKEND_PROXY_TARGET.
# Example to bypass proxy and hit backend directly:
# REACT_APP_API_BASE_URL=http://localhost:8000
REACT_APP_API_BASE_URL=

# Observability/Monitoring feature flags (placeholder for future integration).
# Toggle to enable frontend observability features (e.g., sending logs/metrics to a collector).
# Values: "true" or "false"
REACT_APP_OBS_ENABLED=false

# Base URL for observability backend if enabled (e.g., Monitoring&Logging container).
# Example: http://localhost:9000
REACT_APP_OBS_BASE_URL=http://localhost:9000

# Optional: Override target for CRA dev proxy (used only by `npm start`).
# This is read by src/setupProxy.js. If not set, defaults to http://localhost:8000
# BACKEND_PROXY_TARGET=http://localhost:8001
# BACKEND_PROXY_TARGET=http://localhost:8000
