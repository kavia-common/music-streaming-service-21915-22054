# Environment variables for WebFrontend
# Do not commit a populated .env. Copy this file to .env and fill values as needed.

# Base URL for all API requests. If unset, CRA dev proxy will be used for paths starting with /api.
REACT_APP_API_BASE_URL=

# Optional: override CRA proxy target used by src/setupProxy.js
BACKEND_PROXY_TARGET=http://localhost:8000
