# WebFrontend environment example

# Optional: If set, axios calls the backend directly using this base URL.
# When unset, CRA dev proxy will forward /api/* requests to BACKEND_PROXY_TARGET.
# Example:
# REACT_APP_API_BASE_URL=http://localhost:8000
REACT_APP_API_BASE_URL=

# CRA dev proxy target used by src/setupProxy.js
BACKEND_PROXY_TARGET=http://localhost:8000

# Optional feature flags (reserved for future)
REACT_APP_OBS_ENABLED=false
REACT_APP_OBS_BASE_URL=
