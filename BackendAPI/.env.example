# BackendAPI environment configuration

# Security
SECRET_KEY=devsecret_change_me
JWT_ISSUER=music-streaming-backend

# Database
# Defaults to SQLite if not set. For Postgres, uncomment and set accordingly.
# DATABASE_URL=postgresql+psycopg2://USER:PASS@HOST:5432/DBNAME
DATABASE_URL=sqlite:///./app.db

# CORS (comma-separated origins). Prefer using CRA proxy during dev.
CORS_ORIGINS=

# Server port
PORT=8000
