# BackendAPI environment configuration example

# JWT signing secret (use a strong value in production)
SECRET_KEY=devsecret_change_me

# JWT issuer claim
JWT_ISSUER=music-streaming-backend

# Database URL; defaults to SQLite local file
DATABASE_URL=sqlite:///./app.db

# CORS origins:
# - Comma-separated allowed origins. Leave empty to default to permissive CORS in dev.
# - Example: http://localhost:3000,https://my-frontend.example.com
CORS_ORIGINS=

# HTTP port (used by Docker CMD script, uvicorn default is 8000 if not set)
PORT=8000
