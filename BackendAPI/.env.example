# BackendAPI environment variables

# JWT signing secret (required for production; default provided for local dev)
SECRET_KEY=devsecret_change_me

# JWT Issuer string (used in token claims and validation)
JWT_ISSUER=music-streaming-backend

# Database URL (SQLite by default)
DATABASE_URL=sqlite:///./app.db

# Comma-separated CORS origins. Leave empty when using CRA dev proxy (recommended).
# Example: http://localhost:3000,https://your-domain.com
CORS_ORIGINS=

# HTTP port
PORT=8000
